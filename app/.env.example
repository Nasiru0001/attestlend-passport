# These are automatically read from ../deployments/*.json during local builds.
# Set them explicitly when deploying the Next.js app without those JSON files.
NEXT_PUBLIC_TEST_TOKEN_ADDRESS=
NEXT_PUBLIC_LOAN_BOOK_ADDRESS=
NEXT_PUBLIC_CREDIT_PASSPORT_ADDRESS=
